spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE}
server:
  servlet:
    context-path: /act/api
  cors:
    origins: ${CORS_ORIGINS}

#configuração swagger
springdoc:
  swagger-ui:
    path: /public/swagger